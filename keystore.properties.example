# Copy this file to keystore.properties (gitignored) for local signed release builds.
#
# Preferred: generate the keystore off-tree and push the four GitHub Actions secrets:
#   .github/scripts/create-release-keystore.sh
# That writes ~/.blissdeck-keys/ (keep a backup) and sets SIGNING_KEY,
# SIGNING_STORE_PASSWORD, SIGNING_KEY_ALIAS, SIGNING_KEY_PASSWORD on Bliss-Bass/BlissDeck.
#
# Manual local keystore (only if you are not using the script):
#   mkdir -p keystore
#   keytool -genkeypair -v \
#     -keystore keystore/blissdeck-release.jks \
#     -alias blissdeck \
#     -keyalg RSA -keysize 2048 -validity 10000 \
#     -storetype PKCS12
#
# GitHub Actions secrets (Repository settings -> Secrets and variables -> Actions):
#   SIGNING_KEY            = single-line base64 of the keystore file
#   SIGNING_STORE_PASSWORD = keystore password
#   SIGNING_KEY_ALIAS      = blissdeck
#   SIGNING_KEY_PASSWORD   = key password

storeFile=keystore/blissdeck-release.jks
storePassword=CHANGE_ME
keyAlias=blissdeck
keyPassword=CHANGE_ME
